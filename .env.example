PORT=3000

# PostgreSQL (Render or local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=flora
DB_SSL=false

# Public base URL used in photoURL responses. Set to deployed backend URL on Render.
PUBLIC_BASE_URL=http://localhost:3000
